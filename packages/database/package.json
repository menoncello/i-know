{
  "name": "@i-know/database",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "db:generate": "bun run scripts/generate.ts",
    "db:migrate": "bun run scripts/migrate.ts",
    "db:seed": "bun run scripts/seed.ts",
    "db:studio": "bunx @prisma/client studio",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/**/*.ts scripts/**/*.ts",
    "lint:fix": "eslint src/**/*.ts scripts/**/*.ts --fix",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@types/uuid": "^11.0.0",
    "postgres": "^3.4.5",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
