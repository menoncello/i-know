# Production Environment Configuration
# This file contains production-specific environment variables
# DO NOT commit actual secrets - use environment variables or secret management

# Environment
NODE_ENV=production
ENVIRONMENT=production

# Database Configuration (Production)
# These should be set via environment variables or secret management
DATABASE_URL=${DATABASE_URL}
POSTGRES_HOST=${POSTGRES_HOST}
POSTGRES_PORT=${POSTGRES_PORT}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_DB=${POSTGRES_DB}

# API Configuration
API_PORT=3001
API_HOST=0.0.0.0
API_BASE_URL=https://api.i-know.app

# CORS Configuration (Production)
CORS_ORIGIN=https://i-know.app

# Security (Set via environment variables or secret management)
JWT_SECRET=${JWT_SECRET}
SESSION_SECRET=${SESSION_SECRET}

# External Services (Set via environment variables or secret management)
REDIS_URL=${REDIS_URL}
IMDB_API_KEY=${IMDB_API_KEY}

# Monitoring and Logging
LOG_LEVEL=error
SENTRY_DSN=${SENTRY_DSN}

# Feature Flags
ENABLE_METRICS=true
ENABLE_CACHE=true
ENABLE_RATE_LIMITING=true

# Deployment
DEPLOY_ENVIRONMENT=production
RAILWAY_TOKEN=${RAILWAY_TOKEN}

# Performance Configuration
ENABLE_COMPRESSION=true
CLUSTER_MODE=false
MAX_CONNECTIONS=100
CONNECTION_TIMEOUT=30000